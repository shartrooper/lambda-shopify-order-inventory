{
  "name": "lambda-shopify-order-inventory",
  "version": "1.0.0",
  "description": "Lambda function for storing shopify placed orders",
  "main": "fn.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lambda",
    "shopify"
  ],
  "author": "Marko Michinaux",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
